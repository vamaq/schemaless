#!/usr/bin/env python

from distutils.core import setup

setup(name='configuration',
    version='1.0',
    description='Loads the configuration on import and make it available',
    py_modules=['config'],
    install_requires=[],    
)
